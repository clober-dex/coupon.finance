query getBondPositions($userAddress: String!) {
  bondPositions(where: { user: $userAddress }) {
    id
    user
    amount
    substitute {
      id
      decimals
      name
      symbol
    }
    underlying {
      id
      decimals
      name
      symbol
    }
    expiryEpoch
    expiryTimestamp
  }
}
