query getMarkets ($fromEpoch: String!) {
  markets (where: {epoch_gte: $fromEpoch}) {
    id
    couponId
    orderToken
    takerFee
    quoteUnit
    epoch {
      id
      startTimestamp
      endTimestamp
    }
    quoteToken {
      id
      name
      symbol
      decimals
    }
    baseToken {
      id
      name
      symbol
      decimals
    }
    depths {
      price
      rawAmount
      isBid
    }
  }
}
