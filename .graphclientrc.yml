sources:
  - name: coupon-finance
    handler:
      graphql:
        endpoint: '{context.url:https://dev-subgraph.coupon.finance/subgraphs/name/coupon-subgraph-prod}'
  - name: coupon-finance-point
    handler:
      graphql:
        endpoint: '{context.url:https://dev-subgraph.coupon.finance/subgraphs/name/coupon-point-subgraph-prod}'

documents:
  - ./apis/**/*.graphql
  - ./__tests__/**/*.graphql


codegen:
  contextType: 'MeshContext & { url: string }'
